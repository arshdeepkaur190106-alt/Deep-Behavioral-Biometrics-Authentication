"""
backend package

Marks the 'backend' directory as a Python package so its submodules
(config, database, models, schemas, routes, auth, utils) can be imported
using absolute imports like `from backend.config import settings`.
"""