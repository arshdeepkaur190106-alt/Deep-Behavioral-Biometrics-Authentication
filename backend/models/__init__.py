"""
models package

Contains internal data representation classes (UserModel, AuthHistoryModel,
AlertModel, RiskScoreModel) that define the shape of documents stored in
MongoDB. These are distinct from the API-facing schemas in the `schemas`
package.
"""