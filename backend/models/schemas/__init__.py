"""
schemas package

Contains Pydantic schemas that validate data flowing in and out of the
FastAPI routes (request bodies and response models). These govern the
API contract, separate from how data is actually stored in MongoDB.
"""